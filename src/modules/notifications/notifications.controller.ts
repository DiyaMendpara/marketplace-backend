import { Controller, Get, Param, Put, UseGuards } from '@nestjs/common'; import { UserId } from '../../shared/common/decorators/userId.decorator'; import { JwtAuthGuard } from '../../shared/common/guards/auth.guard'; import { NotificationsService } from './notifications.service';
@Controller('notifications') @UseGuards(JwtAuthGuard) export class NotificationsController { constructor(private readonly notifications: NotificationsService) {} @Get() list(@UserId() userId: string) { return this.notifications.list(userId); } @Put(':id/read') read(@Param('id') id: string, @UserId() userId: string) { return this.notifications.read(id, userId); } }
