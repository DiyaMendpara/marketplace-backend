import { Injectable } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model, Types } from 'mongoose'; import { Notification, NotificationDocument } from './schemas/notification.schema';
@Injectable() export class NotificationsService { constructor(@InjectModel(Notification.name) private readonly model: Model<NotificationDocument>) {} create(userId: Types.ObjectId, title: string, body: string, link?: string) { return this.model.create({ userId, title, body, link }); } list(userId: string) { return this.model.find({ userId }).sort({ createdAt: -1 }).limit(100).lean(); } async read(id: string, userId: string) { return this.model.findOneAndUpdate({ _id: id, userId }, { read: true }, { new: true }).lean(); } }
